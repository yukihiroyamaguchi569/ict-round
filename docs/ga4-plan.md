# GA4 学習・活用計画（めぐる君）

作成日: 2026-07-26
最終更新: 2026-08-08

## 前提・方針

- めぐる君は「ダウンロードしたら電波が悪い場所でも使える」ことが売りのPWA。
- gtag.js にはオフライン時のイベント再送機能がなく、電波の悪い場所で発生したイベントは復旧後も送られず消失する。
  - 細かい行動（チェックリスト入力、写真追加など）のファネル計測は、電波の悪い場所ほどデータが欠けるため、離脱率のように見えてしまう危険がある。
  - アクティブユーザー計測も同様に過小カウントされるため、絶対数ではなく前週比・前月比のトレンドとして見る。
- 最初の目標は「細かいユーザー挙動の全計測」ではなく、**アクティブユーザーの把握と分析**。
- 点検者名・病棟名などの個人情報／医療現場の識別情報は絶対にGA4へ送らない（GA4利用規約にも抵触）。

## 重要な既知事象: 2026-06-17〜07-12 の計測データは無効

独自実装した `gtag` 関数が `dataLayer` に配列(`args`)を push していたため、gtag.js がコマンドとして認識できず、`config`/`event` が全て黙って破棄されていた（[fix: GA4イベントが送信されないバグを修正](https://github.com/yukihiroyamaguchi569/ict-round/commit/d05e5db)、2026-07-12）。

- **計測開始日（6/17）から7/12までのデータは実質存在しない。** 期間を指定して過去データを見る際は7/12以降に絞ること。
- 7/11に環境感染学会のシンポジウムで発表しており、その反響が7/13（月）以降の新規ユーザー増として現れている。バグ修正のタイミングと重なったのは偶然。

## 現状の実装（`src/analytics.ts`）

計測済みイベント:
- `page_view`（gtag標準、SPAなので実質初回のみ）
- `pwa_install`（method: `standalone_first_launch` / `appinstalled`）
- `pwa_install_banner_click`
- `pwa_install_prompt_result`（outcome）
- `pwa_install_banner_dismiss`

`session_start` / `first_visit` / `user_engagement` はGA4が自動収集するため、追加実装なしで既に計測されている。

`src/` で `trackEvent` を呼んでいるのは以下の2経路のみ（2026-08-06時点）。いずれもPWAインストール計測で、アプリ本体の操作は1つも計測していない。

- `src/components/InstallBanner.tsx`: バナー系3イベント（`pwa_install_banner_click` / `pwa_install_prompt_result` / `pwa_install_banner_dismiss`）
- `src/analytics.ts`: `pwa_install`。初回のstandalone起動を検知したとき（`method: standalone_first_launch`）と、`src/main.tsx` の `appinstalled` リスナーから `trackInstallEvent()` が呼ばれたとき（`method: appinstalled`）の2箇所。

拡張計測機能によって以下も自動で収集されている:
- `form_start`: フォームへの入力開始。めぐる君では点検者名の入力にあたるため、**「ラウンドに着手した」の代理指標として使える**。
- `file_download`: ファイルのダウンロード。実体は全て `public/round-checklist-template.xlsx` で、**レポート出力（.docx）ではない**。docxはblob生成のため拡張計測では捕捉されない。
- `scroll` / `click`

## 重要な既知事象: 本番に「ラウンド完了」の計測が存在しない (2026-08-06 判明)

アプリの核心的な成功である「ラウンドを最後まで実施してレポートを出力した」がGA4で計測できていない。

- `round_export` イベントはGA4に届いているが、**全て `localhost` とプレビュー環境からのもの**で本番からは1件もない。このイベントは未マージブランチ `feat/issue-46-merge-departments` にのみ実装されており、mainには入っていないため。
- `file_download` は上記のとおりチェックリストのテンプレート配布であり、代理指標にならない。

この状態ではコホート分析をしても「何をもって定着とみなすか」を定義できず、単なる再訪率しか見られない。**ゴールイベントの実装が、今後の分析すべての前提になる。**

## 重要な既知事象: 開発トラフィックが本番プロパティに混入 (2026-08-06 判明)

2026-07-12〜08-05 のホスト別内訳:

| ホスト | ユーザー | イベント |
|---|---:|---:|
| `ict-round.conect.llc`（本番） | 533 | 8,328 |
| `localhost` | 20 | 307 |
| `*.ict-round-preview.pages.dev`（5ホスト） | 13 | 249 |

ユーザー数で約6%が開発トラフィック。`arguments_fix_test` / `claude_debug_test` / `test_event` といったイベントも残っている。

**分析時は必ずホスト名を `ict-round.conect.llc` に絞ること。** 恒久対策としてGA4のデータフィルタでの除外が必要（未対応）。

## フェーズ1: 設定作業（最優先）— 完了 (2026-07-26)

- [x] 管理 > データ設定 > データ保持を **14ヶ月に変更**（デフォルト2ヶ月だとリテンション分析ができない）。
- [x] レポート > ビジネス目標 > 見込み顧客の発掘 > 概要 で新規ユーザー数の推移を確認。
  - ピークは7/13の86人、7/24は12人と減少傾向に見えたが、`/about`（紹介ページ、学会後にSEO登録・拡充）への一過性アクセスの減少が主因。実際のアプリ本体（`/`）のPVは横ばい。
- [x] レポート > 維持率 でアクティブユーザーの推移（1日/7日/30日）とコホート別ユーザー維持率を確認。

## フェーズ2: 「アクティブユーザー」の定義を理解する — 完了 (2026-07-26)

- GA4のアクティブユーザーは単なる訪問者数ではなく、エンゲージメントのあったユーザー。
- エンゲージメントセッションの条件: 10秒以上の滞在 / キーイベント発生 / 2回以上のページビュー のいずれか。
- めぐる君はSPAでページビューが実質1回のみなので、**滞在10秒が実質的な判定基準**になる点を理解しておく。
- `user_engagement` イベントはタブが閉じる瞬間だけでなく、非表示化時・約10秒おきの定期送信・離脱時（`sendBeacon`）の複数タイミングで送信される。ただしオフラインだとどのタイミングでも送信できず欠測する点は変わらない。
- **DAU/MAU比**はGA4に専用カードがなく、「ユーザーのアクティビティの推移」グラフ（レポートのスナップショット）の1日/30日の数値から自分で計算する。
  - 2026-07-26時点: 1日=10, 7日=128, 30日=364 → DAU/MAU ≈ 2.7%。ラウンド業務は毎日行うものではないため、この低さ自体は問題ではない。
  - 30日線は移動窓の性質上、学会由来の7月前半の高アクセスをまだ含んでおり右肩上がりに見える。**8月中旬ごろにその山が窓から外れ、自然に下降に転じる見込み**（異常ではない）。

## フェーズ3: 定着状況を見る（探索機能）— 実施 (2026-08-06)

当初は8月中旬以降に再開する予定だったが、GA4のMCPサーバーを接続した機会に前倒しで実施した。

### コホートデータ探索: PWAインストールの有無で定着に差があるか

「PWAインストール済み」（`pwa_install` を発火したユーザー）と「PWA未インストール」のセグメントで、獲得週ごとの週次残存率を比較した（期間 2026-07-12〜08-05）。

**PWAインストール済み（n=58）**

| 獲得週 | 週0 | 週1 | 週2 | 週3 |
|---|---:|---:|---:|---:|
| 07/12-18 | 23 | 43.5% | 30.4% | 21.7% |
| 07/19-25 | 16 | 31.3% | 18.8% | — |
| 07/26-08/01 | 10 | 40.0% | — | — |
| 08/02-05 | 9 | 33.3% | — | — |
| **全体** | **58** | **37.9%** (22/58) | **25.6%** (10/39) | **21.7%** (5/23) |

**PWA未インストール（n=532）**

| 獲得週 | 週0 | 週1 | 週2 | 週3 |
|---|---:|---:|---:|---:|
| 07/12-18 | 266 | 12.0% | 10.2% | 5.3% |
| 07/19-25 | 94 | 16.0% | 4.3% | — |
| 07/26-08/01 | 123 | 8.1% | — | — |
| 08/02-05 | 49 | 10.2% | — | — |
| **全体** | **532** | **11.7%** (62/532) | **8.6%** (31/360) | **5.3%** (14/266) |

「全体」行の分母は、**その週まで観測できたコホートだけ**の合計であり、見出しの `n` とは一致しない。たとえば週2の時点で観測できているのは 07/12-18 と 07/19-25 の2コホートのみで、07/26 以降に獲得したユーザーはまだ週2を迎えていないため分母に入らない。

### 分かったこと

- **インストール済みの残存率は未インストールの3〜4倍。** 週1で 37.9% vs 11.7%、週3で 21.7% vs 5.3%。
  - ただし**有意性検定は行わない**。後述のとおり「PWA未インストール」セグメントにインストール済みユーザーが混入しており、2つのセグメントが相互排他的でないため、独立2標本の検定の前提が成立しない。検定するにはセグメントを作り直して取得し直す必要がある。
- **差の大半は初週で決まる。** 未インストール層は週0→週1で約9割、インストール層でも6割が消える。週1を生き延びた後の減衰速度は両者で大きく変わらない（週1→週3で残るのはインストール済み57%、未インストール45%）。**勝負は最初の1週間。**
- 学会（7/11）直後の 07/12-18 コホート266人のうち、週3まで残ったのは14人（5.3%）。反響の実質的な残存はこの規模。

### 解釈上の注意

- **選択バイアスは消えない。** インストールする人はもともと継続利用を決めた人であり、「インストールさせれば定着する」とは読めない。ただし4〜6倍という差の大きさは、インストール導線への投資を正当化する傍証にはなる。
- **母数が小さい。** インストール済みの各コホートは9〜23人。週3の 21.7% は23人中5人で、1人の増減で4.3ポイント動く。後半週は幅を持って見ること。
- **上記のセグメント設定には漏れがあった。** コホート合計590に対し実ユーザーは533で、差の57人はほぼインストール済み（58人）と一致する。つまり「PWA未インストール」セグメントにインストール済みユーザーがほぼ全員含まれていた（除外条件が「完全に除外」として効いていなかった）。次回やり直す際は要修正。
  - ただし**結論の方向は変わらず、差はむしろ過小評価**。純粋な未インストール層を推定すると週1で 8.4%（40/474）、週3で 3.7%（9/243）となり、差は4.5〜5.9倍に広がる。

### ユーザーのライフタイム探索

未実施。ゴールイベントが本番に存在しない現状では「累積利用回数」の意味づけが弱いため、ゴールイベント実装後に行う。

## フェーズ4: 既存データで分かること — 一部実施 (2026-08-06)

### デバイス構成（本番のみ、2026-07-12〜08-05）

| OS / 種別 | ユーザー | セッション |
|---|---:|---:|
| iOS mobile | 230 | 515 |
| Windows desktop | 153 | 246 |
| Macintosh desktop | 84 | 198 |
| Android mobile | 73 | 172 |
| iOS tablet | 22 | 35 |
| Android tablet | 3 | 21 |

iPhone中心。iPadは22人と少ない。デスクトップ（Windows + Mac）が237人と多いのは、`/about/` や `/docs/` の閲覧が含まれるため。

### PWAインストール後の利用にOS差がある

| OS | standalone ユーザー | セッション/ユーザー |
|---|---:|---:|
| iOS | 18 | 1.8 |
| Android | 8 | **5.3** |
| Macintosh | 8 | 1.9 |
| Windows | 3 | 4.7 |

母数最大のiOSでインストール後の利用が薄く、少数のAndroidユーザーが最も濃く使っている。iOSのインストール導線または使い勝手に改善余地がある可能性。ただし母数が小さいため断定はできない。

### その他

- `pwa_install` は本番58ユーザー。インストール導線自体は機能している。

### Issue #45（Androidでカメラが起動しない）の実害測定 — 間接指標のみ

**写真追加が計測されていないため、実害を直接測ることはできない。** 以下は間接指標による検証で、結論を断定するものではない。

Issue #45 の仮説は「standalone PWA でのみ `capture` が効かずギャラリーに落ちる」。これが大量離脱を招いているなら、Android の standalone ユーザーの行動が劣化しているはずだが、**その兆候は観測されない。**

ラウンド着手率（`form_start` 到達ユーザー / `session_start` ユーザー、本番・2026-07-12〜08-05）:

| OS | display_mode | session_start | form_start | 着手率 |
|---|---|---:|---:|---:|
| Android | standalone | 8 | 6 | **75.0%** |
| Android | browser | 9 | 7 | 77.8% |
| Android | (not set) | 67 | 29 | 43.3% |
| iOS | standalone | 13 | 5 | 38.5% |
| iOS | browser | 47 | 22 | 46.8% |
| iOS | (not set) | 210 | 57 | 27.1% |

Android は display_mode を問わず iOS より着手率が高く、**standalone 特有の劣化は見られない**。定着面でも Android のほうが上回る。

| OS | 全ユーザー | 再訪ユーザー | 再訪率 | 再訪時の平均滞在 |
|---|---:|---:|---:|---:|
| Android | 76 | 31 | 40.8% | **10分26秒** |
| iOS | 252 | 80 | 31.7% | 5分23秒 |

Android の再訪ユーザーは1人あたり3.5セッション（iOSは2.5セッション）、`form_start` は standalone で1人あたり6.2回。**カメラバグを抱えたまま最もヘビーに使っている層**という姿になる。

Issue #49（案内文の追加、2026-07-26マージ）の前後比較:

| 期間 | Android 着手率 | iOS 着手率 |
|---|---:|---:|
| 07/12-07/25（案内なし） | 46.8%（29/62） | 31.5%（57/181） |
| 07/27-08/05（案内あり） | 56.5%（13/23） | 30.3%（27/89） |

Androidのみ +9.7ポイント改善しiOSは横ばいだが、**統計的には有意でない**（z ≈ 0.79、p ≈ 0.43）。母数不足のため効果ありとは言えない。

**この測定で言えないこと**:
- **生存者バイアス**。カメラで詰まった人は既に離脱しており、残った層だけを見ている可能性がある。
- **母数が小さい**。Android standalone は8ユーザー。
- **写真を追加できたかは分からない**。着手率が高くても、写真なしでラウンドを終えているかもしれない。

**修正優先度の判断**: 観測できる範囲では大量離脱の証拠がなく、緊急性は低い。ただし「実害がない」ではなく「観測できるほどの離脱は起きていない」に留まる。判断を確定するには写真追加の計測が必要。

**必要な計測**: `photo_add_attempt`（`method: camera | gallery`）と `photo_add_success`（同）の2イベント。「カメラボタンを押したのに写真が付かなかった」「カメラを試してギャラリーに切り替えた」が検出でき、Issue #45 の実害を数値化できる。

### チェックリストをカスタマイズした人の規模（本番のみ、2026-07-12〜08-05）

テンプレート（`round-checklist-template.xlsx`）をダウンロードしたのは **48ユーザー / 112件**。本番533ユーザーの9.0%。

| | ユーザー | 件数 | 1人あたり |
|---|---:|---:|---:|
| 再訪時のDL | 29 | 69 | 2.4回 |
| 初回訪問時のDL | 28 | 43 | 1.5回 |

半分以上が再訪してからダウンロードしており、衝動的でなく検討を重ねた行動として現れている。

| デバイス | DLユーザー | 全体 | DL率 |
|---|---:|---:|---:|
| tablet | 10 | 25 | **40.0%** |
| desktop | 20 | 240 | 8.3% |
| mobile | 19 | 303 | 6.3% |

タブレット（実運用でラウンドに持ち歩く端末）のDL率が突出。実運用に入った人ほどカスタマイズを求める関係が見える。ただし母数25人と小さいため幅を持って見ること。

1人あたり平均2.3回ダウンロードしている。同じテンプレートを繰り返し落とす理由は「編集→取り込み失敗→再DL」のループが考えられるが、これは推測であり確認できていない。

**この先が計測できない。** `src/components/ChecklistImportDialog.tsx` に `trackEvent` の呼び出しは一切なく、取り込みダイアログを開いた回数・パース失敗（同ファイル36-38行目の `setError`）・「保存して適用」への到達はいずれも計測されていない。「48人がカスタマイズを試みた」までは言えるが、「何人が実際に自院用チェックリストを使い始めたか」は分からない。

改善案として、`checklist_import_open` / `checklist_import_error`（エラー内容・拡張子）/ `checklist_import_success`（カテゴリ数・項目数）の3イベントを追加すれば埋まる。`checklist_import_error` は Issue #8 / #18 のCSV形式ヘルプ対応の効果測定にも使える。ゴールイベント実装と同じPRにまとめるのが効率的（どちらも `trackEvent` の追加で影響範囲が重なる）。

### `/about` 着地ユーザーの後続行動（本番のみ、2026-07-12〜08-05）

`/about`（紹介ページ）に着地した285ユーザーのその後:

| 到達点 | ユーザー | 着地からの率 |
|---|---:|---:|
| `/about` 着地 | 285 | 100% |
| アプリ本体 `/` を開いた | 150 | 52.6% |
| ラウンドに着手（`form_start`） | 83 | 29.1% |
| PWAインストール | 23 | 8.1% |
| テンプレートDL | 20 | 7.0% |

紹介ページ経由でも3割が実利用まで到達した。ただし**この285人を学会発表からの流入と識別することはできない**（下記「測定上の限界」参照）ため、学会の効果と読むのは時期の整合による推測にとどまる。参考に、最初から `/` に来た303ユーザーは着手121人（39.9%）、インストール35人（11.6%）で、直接来る人の方が関心が高いのは自然な差。

**最大の離脱点は `/about` → `/` の遷移。** 着地285人のうち135人（47.4%）がアプリを開かずに離脱する一方、アプリを開いた150人のうち83人（55.3%）は着手している。アプリさえ開けば半分以上が使い始めるため、`/about` からアプリへの導線が最も改善余地の大きい箇所。

チャネル別（Direct 913セッション / 432ユーザーが最大、Organic Social 72セッション・25ユーザー、Organic Search 58セッション・30ユーザー）で着地先に傾向がある。SNS（t.co・facebook系）は `/about` に集まり（52ユーザー vs `/about`は`/`より多い）、検索エンジンは `/` に直接来る（23 vs 7）。紹介ページがSNSで共有され、検索ではアプリ本体が引っかかっている構図。

Referral に `192.168.0.254`（院内LAN）と `gw01.kitakyu-cho.jp`（自治体ドメイン）が現れており、現場内でURLが共有されている証拠と読める。

週次の新規ユーザー数（`/about` / `/` 着地の合計）は学会週（7/12-18）の267人から週100〜130人程度に落ち着き、7/26週で持ち直している。減衰は止まりつつある。

**測定上の限界**: Direct が82%を占めるが、これを学会由来と断定できない。PWA起動・ブックマーク・QRコード・メールアプリ経由がすべてDirectに分類され、再訪者も混ざるため。**今後の学会発表・配布資料のURLにはUTMパラメータ（例: `?utm_source=jsipc&utm_medium=slide`）を付けるべき。** 次回の発表分には間に合う。

## 実装済みの追加計測: `display_mode`

インストール版（PWA）からの起動か、ブラウザからの起動かを切り分けて記録する（`src/analytics.ts` で実装済み）。

- `gtag('set', 'user_properties', ...)` でユーザープロパティ `display_mode: 'standalone' | 'browser'` を送信。
- 「アクティブユーザーのうち何%がインストール版か」をレポートで切り分けられる。
- **解釈の注意**: これで分かるのはインストール版の利用割合であり、そのセッションがオフラインだったかどうかは分からない。オフライン利用そのものの指標ではなく、インストールの定着度を見る指標として扱う。
- **レポートで使うにはカスタムディメンション登録が必須**（下記「次のアクション」参照）。登録前に収集されたデータは遡って参照できない。
- **`display_mode` で再訪率を比較してはいけない（2026-08-06 追記）**。`standalone` になるのはPWAとして起動したときだけで、インストールは「1回目はブラウザ → インストール → 2回目以降にPWA起動」という順序を必ず踏む。つまり `standalone` に分類された時点でそのユーザーは定義上ほぼ再訪者であり、再訪率を比較すると分類方法そのものが差を作ってしまう。
  - 実際に `display_mode` で切ると再訪率は standalone 68% / browser 38% と出るが、期間を25日から7日に縮めると standalone だけが58%まで下がる（browser は39%で不変）。この期間依存性がバイアスの証拠。
  - 定着を比較するなら**獲得週で固定したコホート探索**を使うこと（フェーズ3参照）。
- `(not set)` が多いのは、`/about/` や `/docs/` など静的ページのみを訪問した層（アプリ本体を起動していないため計測されない）。

あわせて、開発時のみ `debug_mode` を有効にしている。PWAをDockやホーム画面から起動した場合はChrome拡張のデバッグモードが効かず、GA4のDebugViewに何も表示されないため。本番ビルドでは除去される。

## やらないこと（優先度低 / スコープ外）

- チェックリスト/写真/評価などステップ単位の細かいファネル計測（オフライン欠測の影響が大きいため、いったん見送り）
- Google広告連携、オーディエンス、Attribution、eコマース測定、ユーザーIDによるクロスデバイス計測

## 分析環境: GA4 MCPサーバー (2026-08-06 接続)

Google公式の [googleanalytics/google-analytics-mcp](https://github.com/googleanalytics/google-analytics-mcp)（Apache 2.0）をClaude Codeに接続済み。管理画面を操作せずにレポートを引ける（Issue #76）。

- 認証方式は quickstart が desktop OAuth / web OAuth / サービスアカウントの impersonation の3通りを示しており、今回はADC（desktop OAuth）を選んだ。
  - **本環境での観測（一般的な要件ではない）**: `gcloud auth application-default login` の標準OAuthクライアントでは `analytics.readonly` スコープがGoogle側に拒否されたため、GCPプロジェクトにデスクトップ用OAuthクライアントを自前で作成し、そのクライアントを指定してADCを取り直すことで解決した。エラー原文とgcloud/MCPのバージョンは記録し損ねているため、他の環境で同じことが起きるかは不明。
- 設定は `claude mcp add --scope user`（`~/.claude.json`）。**認証情報もプロパティIDもリポジトリには置かない。**
- **できないこと**: `run_report` は Segment / segmentFilterExpression を受け付けないため、「特定のイベントを発火したユーザー」を切り口にしたレポート比較はMCP経由では作れない。
  - `run_funnel_report` は例外で、`funnel.segments` として `userSegment` / `sessionSegment` / `eventSegment` を渡せる（Data API v1alpha、最大4セグメント）。ただし返ってくるのはファネル各ステップの通過数であり、**獲得週で固定したコホートの週次残存率表は作れない**。
  - したがって今回のPWAコホート分析には、引き続きGA4の探索機能かBigQueryエクスポートが必要。

## 次のアクション

1. ~~GA4管理画面でデータ保持期間を14ヶ月に変更~~ → 対応済み。
2. ~~GA4管理画面で `display_mode` をカスタムディメンションとして登録する~~ → 対応済み（2026-07-26）。
3. ~~フェーズ3のコホート探索で定着状況を確認する~~ → 対応済み（2026-08-06）。
4. **ゴールイベントを実装する（最優先）**。「レポート出力」「共有」など、ラウンド完了を示すイベントをmainに入れる。これがないと定着の定義ができず、以降の分析が意味を持たない。あわせて以下も同PRでまとめる。
   - `ChecklistImportDialog.tsx`: `checklist_import_open` / `checklist_import_error` / `checklist_import_success`
   - `PhotoForm.tsx`: `photo_add_attempt` / `photo_add_success`（いずれも `method: camera | gallery`）— Issue #45 の実害測定に必要
5. **GA4のデータフィルタで開発トラフィックを除外する。** `localhost` と `*.ict-round-preview.pages.dev` を対象にする。
   - まず `Testing` 状態で作成し、`localhost` とプレビューのイベントが除外対象として拾われること、および `ict-round.conect.llc` のイベントが残ることを確認してから `Active` に切り替える。
   - データフィルタは**適用後に収集されるデータにのみ効き、除外されたデータは恒久的に失われる**（後から取り消しても戻らない）。過去に混入した `localhost` / プレビューのイベントもフィルタでは消えないため、過去分の分析ではこれまでどおりホスト名を `ict-round.conect.llc` に絞って読む。
6. 8月中旬、学会由来のスパイクが30日集計から抜けた頃に「ユーザーのアクティビティの推移」を再確認し、定常状態のDAUを基準値として記録する。
7. ゴールイベント実装後、ユーザーのライフタイム探索で累積利用回数・ヘビーユーザーを把握する。
8. **初週の脱落を減らす施策を検討する。** フェーズ3で「勝負は最初の1週間」と分かったため、ここが最も効果の見込める箇所。
9. **`/about` からアプリ本体への導線を改善する。** 着地した紹介ページ訪問者の47.4%がアプリを開かずに離脱している一方、開いた人の55.3%は着手まで進んでいる。
10. **今後の学会発表・配布資料のURLにUTMパラメータを付ける。** 現状Direct（82%）に学会由来と再訪者が混在し、流入元を分離できない。
